# Agent / editor
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Build / deps
node_modules/
.cache/
.parcel-cache/
# dist/ is committed on purpose — it's the precompiled CSS the static site serves.

# Env
.env
.env.local

# Raw photo source folder (normalized copies stored in fotos/)
fotos nuevas/
